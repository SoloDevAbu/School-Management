-- Create initial admin user (run this after setting up the database)
-- This script will be executed to create the first admin user

-- Note: This is a placeholder. In production, you should use the API endpoint
-- /api/auth/seed-admin to create the first admin user securely
